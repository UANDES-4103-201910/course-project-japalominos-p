<%= form_with(model: image_dump, local: true) do |form| %>
  <% if image_dump.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(image_dump.errors.count, "error") %> prohibited this image_dump from being saved:</h2>

      <ul>
      <% image_dump.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
