<%= form_with(model: dump, local: true) do |form| %>
  <% if dump.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dump.errors.count, "error") %> prohibited this dump from being saved:</h2>

      <ul>
      <% dump.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
