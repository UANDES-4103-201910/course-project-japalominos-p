<%= form_with(model: comment_of_comment, local: true) do |form| %>
  <% if comment_of_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment_of_comment.errors.count, "error") %> prohibited this comment_of_comment from being saved:</h2>

      <ul>
      <% comment_of_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
